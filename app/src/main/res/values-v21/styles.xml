<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"></style>

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorSecondary">@color/colorOverflow</item>
    </style>


    <!-- OUR CUSTOM STYLES -->
    <style name="BadgerActionBarTheme" parent="Theme.AppCompat.Light">
        <item name="android:textColorSecondary">@color/colorOverflow</item>
    </style>

    <style name="BadgerButton">
        <item name="android:background">@color/buttonColor</item>
        <item name="android:textColor">@android:color/background_light</item>
    </style>

    <!-- Modify this for the default bg for the app -->
    <style name="BadgerLayout">
        <item name="android:background">@drawable/badgerbg</item>
    </style>

</resources>
